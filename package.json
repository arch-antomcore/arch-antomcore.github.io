{
  "name": "aethersite",
  "version": "1.0.0",
  "private": true,
  "description": "AetherCore — site estático com HTML/CSS/JS, GSAP, ScrollTrigger e WebGL.",
  "scripts": {
    "preview": "python -m http.server 3001",
    "preview:py": "py -m http.server 3001",
    "compress": "node tools/compress-images.mjs"
  },
  "dependencies": {
    "sharp": "^0.34.5"
  }
}
